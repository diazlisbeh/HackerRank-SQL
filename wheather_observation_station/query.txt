SELECT CITY, LENGTH(CITY) 
FROM (SELECT CITY FROM STATION ORDER BY LENGTH(CITY),CITY) 
WHERE ROWNUM <2;
       
SELECT CITY, LENGTH(CITY) 
FROM (SELECT CITY FROM STATION ORDER BY LENGTH(CITY) DESC,CITY DESC) 
WHERE ROWNUM <2;
     
